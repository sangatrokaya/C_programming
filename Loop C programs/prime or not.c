// C program to check whether a given number is prime or not
// A Prime Number is a positive integer greater than 1 which is divisible by 1
#include<stdio.h>
int main ()
{
    int n, i;
    printf("Enter an integer number: ");
    scanf("%d", &n);

    for (i=2; i<=n;)
}